{
  "name": "habit-web",
  "private": true,
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "dev": "pnpm -r --parallel dev",
    "build": "pnpm -r build",
    "start": "pnpm --filter server start"
  },
  "devDependencies": {
    "@types/node": "^24.3.0",
    "npm-run-all": "^4.1.5"
  }
}
